A simple example of how to setup a react context with createContext and useReducer to give access of actions to each components inside the CounterProvider.

This example use TS to type the context well.